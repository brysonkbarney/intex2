@page
@model intex2.Pages.CartModel
@{
    ViewData["Title"] = "Cart";
    var total = Model.Cart?.CalculateTotal() ?? 0m;
}

<h1>Your Cart</h1>

<table class="table table-bordered table-striped">
    <thead>
    <tr>
        <th class="text-center">Product</th>
        <th class="text-center">Quantity</th>
        <th class="text-center">Amount</th>
        <th class="text-center">Subtotal</th>
        <th></th>
    </tr>
    </thead>
    
    <tbody>
    @foreach (var l in Model.Cart?.Lines ?? Enumerable.Empty<Cart.CartLine>())
    {
        <tr>
            <td class="text-center">@l.Product.Name</td>
            <td class="text-center">@l.Quantity</td>
            <td class="text-center">$@l.Product.Price</td>
            <td class="text-center">$@((l.Quantity* l.Product.Price).ToString())</td>
            <td class="text-center">
                <form asp-page-handler="Remove" method="post">
                    <input type="hidden" name="ProductId" value="@l.Product.ProductId"/>
                    <input type="hidden" name="returnUrl" value="@Model?.ReturnUrl"/>
                    <button type="submit" class="btn btn-sm btn-danger">Remove</button>
                </form>
            </td>
        </tr>
    }
    </tbody>
    <tfoot>
    <tr>
        <td colspan="3" class="text-right">Total:</td>
        <td class="text-center">@total.ToString("c")</td>
    </tr>
    </tfoot>
</table>

<div class="text-center">
    <br></br>
    @if (total > 0)
    {
            <form asp-controller="Home" asp-action="CheckoutConfirmationStart" method="post">
                <input type="hidden" name="amount" value="@Model.Cart?.CalculateTotal()"/>
                <button type="submit" class="btn btn-primary">Proceed to Checkout</button>
            </form>
    }
    <br></br>
    <a class="btn btn-primary" asp-controller="Home" asp-action="Shop">Continue Shopping</a>
</div>




